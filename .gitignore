# dependencies
node_modules/

# secrets
.env
.env.local

# runtime state — the browser session and per-showtime alert state are
# machine-specific and shouldn't be committed
data/

# build output
dist/
*.tsbuildinfo

# logs
*.log

# macOS
.DS_Store

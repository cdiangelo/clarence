{
  "name": "seat-drop-watcher",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Polls a Cinemark seat map and pushes a phone alert the moment a seat opens up on a sold-out showtime.",
  "scripts": {
    "login": "tsx src/login.ts",
    "watch": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "playwright": "^1.48.0",
    "yaml": "^2.6.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
